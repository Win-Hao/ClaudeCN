• 修复个别 Mac 用户「汉化后重启 Claude 白屏」的问题：新版 Claude 把语言列表挪进了哈希命名的 chunk，旧逻辑没把 zh-CN 登记进去，某些系统/账号语言下界面会卡在未加载（白屏）且无任何报错
• 现在为所有可能的中文 locale 写齐译文文件、并在换入前自动自检，从根上杜绝白屏；换入失败会干净中止、绝不留下坏掉的 Claude
• 一键恢复更稳：还原英文原版时同步撤销语言注入，避免「还原后反而白屏」
• 同步修复 Windows 端同源问题（语言列表 chunk 注入 + 别名兜底 + 恢复清理）
